<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/bg_main">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_margin="16dp"
        app:cardCornerRadius="40dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="#FFFFFF">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- Botón configuración -->
            <androidx.appcompat.widget.AppCompatImageButton
                android:id="@+id/btnSettings"
                android:layout_width="52dp"
                android:layout_height="52dp"
                android:layout_alignParentTop="true"
                android:layout_alignParentStart="true"
                android:layout_margin="24dp"
                android:background="@drawable/bg_settings_icon"
                android:src="@drawable/ic_settings"
                android:padding="14dp"
                android:tint="@color/settings_icon"
                android:contentDescription="@string/settings_desc" />

            <!-- Título y subtítulo -->
            <LinearLayout
                android:id="@+id/headerLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_centerHorizontal="true"
                android:layout_marginTop="120dp"
                android:orientation="vertical"
                android:gravity="center">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Memory App"
                    android:textSize="28sp"
                    android:textStyle="bold"
                    android:textColor="@color/title_color" />

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="@string/choose_category"
                    android:textSize="16sp"
                    android:textColor="@color/subtitle_color" />

            </LinearLayout>

            <!-- Botones de categoría -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_centerInParent="true"
                android:layout_marginHorizontal="32dp"
                android:orientation="vertical">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnAnimales"
                    android:layout_width="match_parent"
                    android:layout_height="84dp"
                    android:layout_marginBottom="19dp"
                    android:background="@drawable/bg_button_animales"
                    android:elevation="4dp"
                    android:text="@string/category_animales"
                    android:textAllCaps="false"
                    android:textColor="#FFFFFF"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    app:backgroundTint="@null"
                    app:cornerRadius="32dp"
                    app:icon="@drawable/ic_paw"
                    app:iconGravity="textStart"
                    app:iconPadding="12dp"
                    app:iconTint="#FFFFFF" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnObjetos"
                    android:layout_width="match_parent"
                    android:layout_height="84dp"
                    android:layout_marginTop="20dp"
                    android:layout_marginBottom="19dp"
                    android:background="@drawable/bg_button_objetos"
                    android:elevation="4dp"
                    android:text="@string/category_objetos"
                    android:textAllCaps="false"
                    android:textColor="#FFFFFF"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    app:backgroundTint="@null"
                    app:cornerRadius="32dp"
                    app:icon="@drawable/ic_shapes"
                    app:iconGravity="textStart"
                    app:iconPadding="12dp"
                    app:iconTint="#FFFFFF" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnColores"
                    android:layout_width="match_parent"
                    android:layout_height="84dp"
                    android:layout_marginTop="20dp"
                    android:background="@drawable/bg_button_colores"
                    android:elevation="4dp"
                    android:text="@string/category_colores"
                    android:textAllCaps="false"
                    android:textColor="#FFFFFF"
                    android:textSize="24sp"
                    android:textStyle="bold"
                    app:backgroundTint="@null"
                    app:cornerRadius="32dp"
                    app:icon="@drawable/ic_palette"
                    app:iconGravity="textStart"
                    app:iconPadding="12dp"
                    app:iconTint="#FFFFFF" />

            </LinearLayout>

        </RelativeLayout>

    </androidx.cardview.widget.CardView>

</RelativeLayout>